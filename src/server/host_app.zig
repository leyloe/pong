const net = @import("net.zig");
const host_game = @import("host_game.zig");
const app = @import("../app.zig");

const rl = @import("raylib");

pub const HostApp = struct {
    const Self = @This();
};
